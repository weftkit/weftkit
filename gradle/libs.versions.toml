[versions]
auto-service = "1.1.1"
junit = "5.11.4"
junit-platform = "1.11.4"
maven-publish = "0.34.0"
spotless = "8.8.0"

[libraries]
# The Bukkit API is only published as snapshots; the timestamped build is immutable, so the
# compatibility floor cannot silently change or break reproducibility (last 1.8-R0.1 build)
bukkit = { module = "org.bukkit:bukkit", version = "1.8-R0.1-20150227.224714-194" }
auto-service = { module = "com.google.auto.service:auto-service", version.ref = "auto-service" }
auto-service-annotations = { module = "com.google.auto.service:auto-service-annotations", version.ref = "auto-service" }
junit-jupiter = { module = "org.junit.jupiter:junit-jupiter", version.ref = "junit" }
junit-platform-launcher = { module = "org.junit.platform:junit-platform-launcher", version.ref = "junit-platform" }
compile-testing = { module = "com.google.testing.compile:compile-testing", version = "0.21.0" }

[plugins]
maven-publish = { id = "com.vanniktech.maven.publish", version.ref = "maven-publish" }
spotless = { id = "com.diffplug.spotless", version.ref = "spotless" }
