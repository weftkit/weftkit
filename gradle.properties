group=org.weftkit
# Default for local builds; the release workflow overrides this with -Pversion= from the tag
version=0.1.0-SNAPSHOT

org.gradle.parallel=true
org.gradle.caching=true
org.gradle.configuration-cache=true
